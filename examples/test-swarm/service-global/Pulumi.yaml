name: docker-service-gloabl
runtime: yaml
resources:
  service:
    type: docker:Service
    properties:
      taskSpec:
        containerSpec:
          image: nginx
      mode:
        global: true
